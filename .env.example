# Copy this file to `.env` and fill in your own values.
# `.env` is git-ignored. Never put real secrets in this file.
#
# NOTE: the app does not read any environment variables yet.
# This is a placeholder for the model provider key that will be needed later.

# API key for the model provider (placeholder, not a real key)
ANTHROPIC_API_KEY=your-api-key-here
